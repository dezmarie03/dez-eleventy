{% for project in projectlist %}
  {% if loop.first %}<ul class="plain-list grid">{% endif %}
    <li class="postlist-item{% if post.slug == slug %} postlist-item-active{% endif %}">
      <a href="/projects/{{ project.slug }}" class="postlist-link">
        <div>
          <div class="grid-image" style="background-color: {{ project.coverImg.responsiveImage.bgColor }};">
            <picture>
              <source media="(min-width: 18em)" srcset="{{ project.coverImg.responsiveImage.srcSet }}">
              <img src="{{ project.coverImg.responsiveImage.src }} " alt="{{ project.coverImg.alt }}" style="width: auto;" lazy="true">
            </picture>
          </div>
          <div class="grid-description">
            <h5>{{ project.title }}</h5>
            <p>{{ project.seoMeta.description }}</p>
          </div>
        </div>
      </a>
    </li>
  {% if loop.last %}</ul>{% endif %}
{% endfor %}
